# Quantitative Multi-Asset Financial Intelligence & Backtesting Platform
# Install:  python -m pip install -r requirements.txt
# (use "python -m pip", not bare "pip", if Application Control blocks pip.exe)

# --- core (required by every layer) ---
pandas>=2.0.0
numpy>=1.24.0

# --- layer 1: ingestion ---
yfinance>=0.2.40

# --- layer 4: Streamlit dashboard ---
streamlit>=1.32.0
plotly>=5.18.0

# --- REST backend (server.py) ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0

# sqlite3, json, sqlite storage, itertools and argparse are stdlib.
